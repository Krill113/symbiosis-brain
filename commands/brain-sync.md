---
description: Sync the Symbiosis Brain vault to GitHub (commit + push)
allowed-tools: Bash(bash ~/.claude/hooks/brain-sync.sh manual)
---

Manual vault sync — the same script SessionEnd runs, but in verbose mode: git output
is shown and failures are not silenced.

!`bash ~/.claude/hooks/brain-sync.sh manual`
